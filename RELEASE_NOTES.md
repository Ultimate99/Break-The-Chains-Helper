v5.6.0 — Custom Dark Window Chrome

- Removes the bright native Windows title bar so the Arena Dashboard stays visually continuous.
- Adds a dark in-app title bar matching the Figma dashboard.
- Adds custom minimize, maximize/restore, and close controls.
- Drag the title bar to move the window; double-click it to maximize/restore.
- Keeps Windows taskbar / Alt-Tab presence with the borderless window.
- Preserves resizing through subtle side and bottom resize hit areas.
- Builds while hidden so the old white native caption does not flash at startup.
- Keeps the v5.5 dashboard, GitHub updater, Fast Vision, OCR, Smart Dodge, analytics, and permanent AppData profile intact.
